- config_location.txt to a json config file
- add folder sort by date, non-blocking with cache
- add sort by size (and calculate folder size, cached)
- cache dropbox only/uploaded status, and immediately stop if any diffs found (does hash exist?)
- add large folder load partial, update with javascript, with cache (and loading display in javascript)
- add filetype default icons
- add download folder as zip
- add search
- implement upload to folder
- add logging in python console (pipe to browser?)
- add image preview with cache
- add openmap/googlemap image location visualization
- improve audio/video streaming behaviour