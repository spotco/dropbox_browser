bottom panel allow drag to top (video player full screen does this)
bottom panel top bar allow min/max
load playlist -> always "songs"
export playlist -> always dropbox_browser_playlists.json, make it different for every user
video player full browser - esc to return

recently played: include playlist in entry, resume state at playlist entry when selecting

plans/2026-07-05/PLAN_VIDEO_SUBTITLE_STYLING.md

---

plans/2026-06-04/SPEC_RECURSIVE_FILE_SEARCH.md
	performance improvements

- add openmap/googlemap image location visualization (for camera uploads)

- music player:
	intensity graph of song (if window focused)