- anime player with switch audio/subtitles in browser

	x UI: loading, don't change play/pause status.
	x UI: loading display overlay over video player 
	x UI: overlay controls still show when mouse off
	x UI: save subtitle/audio track selection matching text
	x misc: bottom pane when pulled up to top, scrollbar goes past 
	- playback: use burn-in route for WebVTT (full screen + pip-mode)
	- full screen, playback scrubber does not work

- library management
- forward/backward playlist

---

plans\SPEC_RECURSIVE_FILE_SEARCH.md
	performance improvements

- add openmap/googlemap image location visualization (for camera uploads)

- music player:
	intensity graph of song (if window focused)