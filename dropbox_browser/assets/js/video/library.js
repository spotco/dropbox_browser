/**
 * Legacy flat video library UI — retired in Phase 5.
 * Video hosts shared media-library/library.js via video.js + media-library-bridge.js.
 * Kept as a no-op so old import paths do not hard-fail if referenced.
 */
export function initLibrary(_ctx) {
  // no-op
}
